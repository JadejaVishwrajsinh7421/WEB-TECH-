// What is an Array?
// An array is a special variable that can hold multiple values in a single name.

let fruits = ["apple", "banana", "mango"];

//genral java method 
let numbers = new Array(10, 20, 30);


// Accessing Array Elements

let arr = [100, 200, 300, 400];

console.log(arr[0]); // 100 (first element)
console.log(arr[1]); // 200 (second element)
console.log(arr[3]); // 400 (fourth element)

// modify (update)
 let colors = ["red", "green", "blue"];

colors[1] = "yellow"; // change "green" → "yellow"

console.log(colors); // ["red", "yellow", "blue"]